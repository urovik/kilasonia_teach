import { ISection } from '../types';

export const sectionsData: ISection[] = [
  {
    id: 'C',
    title: 'язык C',
    icon: 'C',
    description: 'Полное руководство по языку Си',
    chapters: [
      {
        id: 'C-basics',
        title: 'Основы C',
        description: 'Изучи базовые концепции языка',
        lessons: [
          {
            id: 'C-intro',
            title: 'Введение в C',
            content: [
              'C — это язык программирования, который предназначен для написания низкоуровневых вещей.',
              'Он работает супер быстро засчет своей низкоуровневости .',
              'Сегодня мы изучим основы: переменные, типы данных, функции.'
            ],
            codeExamples: [
              {
                title: 'первая программа',
                language: 'C',
                code: `#include <stdio.h>\nint main(void){\n\tprintf("Hello, World!");\n}`
              }
            ]
          },
          {
            id: 'C-variables',
            title: 'Переменные и типы данных',
            content: [
              'Начнем с того, что язык C строготипизирован, следовательно, передем тем как задать имя переменной, мы указываем тип, который она может в себе хранить ',
              'Ниже в коде мы можем увидеть как это выглядит в языке C:',
              
            ],
            codeExamples: [
              {
                title: 'Объявление переменных',
                language: 'C',
                code: `#include <stdio.h>\nint main(void){\n\t//переменная с типом int\n\tint number = 0;\n}`
              }
            ]
          },
          {
            id: 'C-types',
            title: 'Типы данных в си',
            content: [
              'В языке си есть различные типы данных, ниже мы перечислим их все',
              'Запоминать все не нужно, из основных сейччас это тип int, char, float и пожалуй больше нам пока в данный момент и не надо',
              'Типы данных в си:',
              'char — 1 байт, диапазон: -128 до 127 (signed) или 0 до 255 (unsigned)',
              'short — 2 байта, диапазон: -32,768 до 32,767',
              'int — 4 байта, диапазон: -2,147,483,648 до 2,147,483,647',
              'long — 4 или 8 байт (зависит от платформы)',
              'long long — 8 байт, диапазон: -9,223,372,036,854,775,807 до 9,223,372,036,854,775,807',
              'Вещественные типы:',
              'float — 4 байта, точность: ~7 знаков',
              'double — 8 байт, точность: ~15 знаков',
              'long double — 10+ байт, точность: ~19 знаков',
              'Специальные типы:',
              'void — обозначает отсутствие значения',
              'Важно помнить, что размеры типов могут отличаться на разных платформах, поэтому для того чтобы узнать размер типа, пользуемся функцией sizeof(название типа)'
            ],
            codeExamples: [
              {
                title: 'Объявление функции',
                language: 'C',
                code: `#include <stdio.h>\n\nint main() {\n    printf("char: %zu байт\\n", sizeof(char));\n    printf("short: %zu байт\\n", sizeof(short));\n    printf("int: %zu байт\\n", sizeof(int));\n    printf("long: %zu байт\\n", sizeof(long));\n    printf("long long: %zu байт\\n", sizeof(long long));\n    printf("float: %zu байт\\n", sizeof(float));\n    printf("double: %zu байт\\n", sizeof(double));\n    return 0;\n}`
              }
            ]
          }
        ]
      },
      {
        id: 'C-advanced',
        title: 'Продвинутое руководство по языку Си',
        description: 'Глубокое погружение в возможности языка',
        lessons: [
          {
            id: 'С-memory',
            title: 'Работа с памятью',
            content: [
              'В языке C можно управлять памятью, это очень важно понимать, потому что в C нету сборщика мусора, всю выделенную память, нужно обрабатывать самому',
              'в этой теме мы рассмотрим, функции который позволяют нам выделить память под определенный тип данных',
              'Важно понимать что в C, без выделения памяти и указателей, никуда, иначе это был бы не Си',
              `Есть 4 основные функции для работы с памятью, которые находятся в стандартной библиотке <stdlib.h>,
              это malloc(), calloc(), realloc(), free().`,
              'Начнем с функции void *malloc(size_t size) - принимает в себя размер типа данных, и возращает указатель в памяти на него',
              'void *calloc(size_t nmemb, size_t size) - принимает два аргумента nmemb - это количетсво элементов, size - размер типа данных, функция вернет указатель на тип данных, с тем количеством элементов, которые мы ей указали\nважно понимать что calloc инициализирует эти элементы 0-лём.',
              'void *realloc(void *ptr, size_t size) - принимает первым аргументом старый указатель, а также размер типа данных, используется чтобы перевыделить память, например в массиве чисел, сделава массив из 4 чисел, в массив где чисел уже 5, функция также возращает указатель на новый кусок памяти',
              'void free(void *ptr) - освобождает выделенную память, если говорить системно, то помечает ее неиспользуемой, принимает в себя указатель на кусок памяти, который нужно осободить',
            ]
          },
        ]
      }
    ]
  },
  
];

// Генерация навигации
export const getNavItems = () => {
  return sectionsData.map(section => ({
    id: section.id,
    label: section.title,
    path: `/section/${section.id}`
  }));
};